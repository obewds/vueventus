// ./tests/validators/ValidUrlDecorations.test.js

import { ValidUrlDecorations } from '../../src/index'


test('ValidUrlDecorations component returns a truthy value', async () => {

    expect(ValidUrlDecorations).toBeTruthy()

})
